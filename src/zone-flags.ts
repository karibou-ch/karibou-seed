/**
 * Zone.js flags - must be loaded before zone.js
 * Optimizations for better performance
 */

// Disable patching requestAnimationFrame for better performance
(window as any).__Zone_disable_requestAnimationFrame = true;

// Disable patching specified eventNames for performance optimization
(window as any).__zone_symbol__UNPATCHED_EVENTS = ['scroll', 'mousemove'];

