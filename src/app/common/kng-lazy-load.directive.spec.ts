import { KngLazyLoadDirective } from './kng-lazy-load.directive';

describe('KngLazyLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new KngLazyLoadDirective();
    expect(directive).toBeTruthy();
  });
});
